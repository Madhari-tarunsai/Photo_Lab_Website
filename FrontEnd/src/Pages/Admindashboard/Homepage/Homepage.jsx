import React from "react";
import "./Homespage.css"; // CSS file

const Homepage = () => {
  return (
    <div className="homepage">
      {/* Hero Section */}
      <section className="hero">
        <h1 className="hero-title">Welcome to Admin Panel</h1>
        <p className="hero-subtitle">
          Manage your posts, monitor users, and stay in control.
        </p>
      </section>

      
    </div>
  );
};

export default Homepage;
